Implement authentication to identify users and authorization checks to ensure users can only access chats they own or are authorized to view.  The `getChatWithMessages` function in `src/lib/chat-actions.ts` needs to verify the user's access rights before returning the chat data.